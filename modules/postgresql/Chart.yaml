apiVersion: v2
name: postgresql
description: A Helm chart for PostgreSQL with dependencies
dependencies:
  - name: postgresql
    version: "16.0.2"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled  # Add conditions for enabling/disabling dependencies
version: 16.0.2
appVersion: "13"
type: application
