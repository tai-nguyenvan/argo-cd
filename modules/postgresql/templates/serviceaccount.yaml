serviceAccount:
  create: true  # This will ensure Helm creates a ServiceAccount
  name: postgresql-service-account  # Optional: name of the ServiceAccount
  automount: true  # Optional: allows the service account token to be automatically mounted
  annotations: {}  # Optional: add custom annotations here
