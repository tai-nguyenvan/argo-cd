apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: postgres-creds
  namespace: development
spec:
  encryptedData:
    # Replace these placeholder values with the output from kubeseal (see README below)
    POSTGRES_USER: PLACEHOLDER_SEALED_POSTGRES_USER
    POSTGRES_PASSWORD: PLACEHOLDER_SEALED_POSTGRES_PASSWORD
    POSTGRES_DB: PLACEHOLDER_SEALED_POSTGRES_DB
  template:
    metadata:
      name: postgres-creds
      namespace: development

# NOTE:
# This is a template only. To create a real SealedSecret, run the kubeseal
# command locally (instructions in README next to this file). Do NOT commit
# plain Kubernetes Secrets into the repository.

